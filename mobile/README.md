# GatePilot Mobile

Mobile client skeleton. Android support starts at Android 9.

- Android 9 corresponds to API level 28.
- `mobile/android/app/build.gradle.kts` sets `minSdk = 28`.

Flutter is the intended cross-platform app framework, while the Android folder also keeps the min-SDK gate explicit for native build checks.
